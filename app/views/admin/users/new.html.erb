<h1>User Registration For Admin</h1>

<%= form_for [:admin,@user] do |f| %>
<%= render :partial => 'shared/form',  :locals => {:f=>f} %>
<%end%>
<td><%= link_to 'Back', admin_users_path %></td>

<%= link_to('Logout',destroy_user_session_path, :method => :delete) %>